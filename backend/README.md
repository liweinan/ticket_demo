# 单体 backend 已迁移至微服务模块，请勿在此目录启动。
# 请使用根目录 `mvn package` 或 `docker compose up --build`。
#
# 微服务模块：
#   gateway-service   :8080
#   tenant-service    :8081
#   event-service     :8082
#   order-service     :8083
#   audit-service     :8084
